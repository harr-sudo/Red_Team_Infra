---
# testlab_linux — handlers.

- name: restart docker
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true
  become: true

- name: reload ufw
  community.general.ufw:
    state: reloaded
  become: true
