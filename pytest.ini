[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    cs_contract: CS REST API OpenAPI contract tests (Layer 1.5)
    slow: tests that take > 1s
